pydantic-settings==2.5.2
sqlalchemy==2.0.35
pymysql==1.1.1
# MySQL 8 의 기본 인증 플러그인이 caching_sha2_password 이고, PyMySQL 은 그것을
# 처리할 때 cryptography 를 요구한다. RDS MySQL 8.4 도 같은 기본값이다.
cryptography==50.0.0
httpx==0.27.2
# APM. SQLAlchemy·httpx 를 자동 계측한다(D-026). 보고 싶은 것은 "DB 조회부터
# api 호출까지 tick 하나가 얼마나 걸렸나" — 워밍이 늦어지는 원인을 DB 쪽과
# api 쪽으로 가른다.
ddtrace==4.13.1
