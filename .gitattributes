# 줄바꿈은 저장소 안에서 LF 하나로 고정한다.
#
# 이게 없으면 Windows 체크아웃이 CRLF 로 받고, 그 상태로 apply 한 Datadog
# Monitor message·대시보드 note 가 CRLF 로 저장된다. macOS 에서 plan 하면
# 글자는 같은데 13개 리소스가 통째로 diff 로 잡히고, 서로 apply 할 때마다
# 왕복한다 (2026-08-20 실제로 겪음).
#
# 바이너리는 text=auto 가 알아서 가려낸다.
* text=auto eol=lf
